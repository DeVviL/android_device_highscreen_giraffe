# giraffe storage specific rc file.

import /init.giraffe_common.rc

on init
    symlink /sdcard /storage/sdcard0

    # By default, primary storage is physical
    #setprop ro.vold.primary_physical 1
